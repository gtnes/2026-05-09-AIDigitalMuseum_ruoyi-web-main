# 功能说明

## 页面访问地址

### 1. AI博物馆首页

- **访问地址**：`http://localhost:5173/museum?id=2095709997836533762`
- **访问方式**：免登录，直接访问
- **参数说明**：
  - `id`（必填）：博物馆 ID，不同 ID 对应不同博物馆的数据
- **页面功能**：
  - 打开时校验服务是否到期：到期显示提示文案（如"用户您好，本产品使用期限已到……"）；未到期正常展示
  - AI馆员：按博物馆配置的智能体（chatapps）轮播展示，背景为背景图、人物为待机形象；点击顶部圆圈或鼠标/触摸左右滑动切换
  - 与AI通话：点击"与{职责名称}通话"跳转到对应的单智能体对话页（`/app-chat?appId=xxx`）
  - AI视频：非遗视频列表（"全部"分类 + 双列卡片 + 分享）
  - 虚拟空间：`vrEnable=1` 且配置了 `vrUrl` 时显示，内嵌 iframe 加载 VR 地址
  - 右上角分享：底部弹出分享抽屉（复制链接 / 微信 / 朋友圈）

### 2. 多智能体对话页面（需要登录）

- **访问地址**：`http://localhost:5173/apps-chat`
- **访问方式**：**必须登录**。未登录时页面打开并弹出登录界面（登录界面不可关闭，支持账号密码登录与邮箱注册），登录成功后弹窗自动关闭、应用列表自动加载；全程不跳转其他页面
- **参数说明**：无
- **页面功能**：
  - 可选择智能体（模型）：输入框左上方应用选择按钮，弹出列表切换，切换后清空对话并重建会话
  - 右上角"退出"按钮（仅登录状态显示）：清除登录凭证后停留在本页，并重新弹出登录界面
  - 左上角返回按钮

### 3. 单智能体对话页面

- **访问地址**：`http://localhost:5173/app-chat?appId=2089924426342092801`
- **访问方式**：**免登录**，但必须携带 `appId` 参数（对外分享入口，如博物馆"与AI通话"）
- **参数说明**：
  - `appId`（必填）：智能体应用 ID，页面仅加载并对话该应用
- **异常处理**：
  - 不带 `appId`，或 `appId` 对应的应用不存在时，页面显示"系统错误"提示，不做任何跳转
- **页面功能**：
  - 无模型选择功能，固定使用 URL 中 `appId` 指定的智能体
  - 左上角返回按钮：点击返回上一个页面（如博物馆页）

## 访问入口关系

```
博物馆首页 /museum?id=xxx（免登录）
    └─ 点击"与AI通话" → /app-chat?appId=xxx（免登录，指定智能体对话）
                              └─ 左上角返回 → 回到博物馆页

/apps-chat（需登录） ← 独立入口，未登录就地弹出登录界面
```
