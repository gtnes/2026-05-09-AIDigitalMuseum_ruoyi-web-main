# 功能说明

## app-chat 智能对话应用

### 简介

`app-chat` 是一个基于 AI 应用的智能对话页面，支持通过 URL 直接进入某个应用进行对话，也支持不带应用 ID 进入后手动选择应用。

### 访问方式

#### 1. 普通使用

直接在浏览器中访问以下地址：

```
http://localhost:5173/app-chat
```

进入页面后，系统会自动获取应用列表，并默认选中第一个应用。你可以在页面顶部的下拉框中切换其他应用。

#### 2. 传递应用 ID 使用

如果你希望直接进入某个指定的应用，可以在 URL 中通过 `appId` 参数传入应用 ID：

```
http://localhost:5173/app-chat?appId=2089924426342092801
```

页面会根据 `appId` 自动定位到对应的应用，并隐藏应用选择器。如果传入的应用 ID 不存在，会提示“应用不存在”。

### 使用流程

1. 打开上述任一地址进入 `app-chat` 页面。
2. 页面会自动建立 SSE 连接，用于接收 AI 的流式回复。
3. 在输入框中输入消息后按 `Enter` 键（或点击“发送”按钮）发送消息。
4. AI 返回的内容会通过 SSE 实时显示在对话区域中。
5. 切换应用时，当前对话会被清空，并重新建立会话连接。

### 注意事项

- `appId` 需要是系统中已存在的应用 ID。
- 页面依赖后端 `/system/chatapp/connect/{sessionId}` 接口建立 SSE 连接，请确保后端服务正常运行。
- 每个会话拥有独立的 `sessionId`，刷新页面后会重新生成会话。
