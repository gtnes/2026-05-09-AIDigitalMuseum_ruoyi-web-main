# 提交说明

本项目使用 [Conventional Commits](https://www.conventionalcommits.org/)（约定式提交）规范来约束 Git 提交信息，并通过 `commitlint` 在提交时自动校验。

## 提交信息格式

```
<type>[(scope)]: <subject>
```

- `type`：提交类型，必须
- `scope`：影响范围，可选
- `subject`：简短描述，必须

### 示例

```bash
feat: 智能对话页面接口
feat(app-chat): 智能对话页面接口
fix: 修复登录后跳转失败
```

## 提交类型说明

| 类型 | 使用场景 |
| --- | --- |
| `feat` | 新增功能、页面、接口 |
| `fix` | 修复 bug |
| `docs` | 仅修改文档、注释 |
| `style` | 代码格式调整，不影响逻辑 |
| `refactor` | 重构代码，既不是 feat 也不是 fix |
| `perf` | 性能优化 |
| `test` | 新增或修改测试 |
| `build` | 构建配置、依赖变更 |
| `ci` | CI/CD 配置变更 |
| `chore` | 其他杂项，如配置、脚本调整 |
| `revert` | 回退代码 |

## 如何提交

### 方式一：直接输入提交信息

```bash
git commit -m "feat: 智能对话页面接口"
```

### 方式二：使用 cz-git 交互式提交（推荐）

```bash
git cz
```

根据提示选择 type、scope，填写描述即可。

## 常见问题

### 1. 提交时报 `type may not be empty`

提交信息缺少 `type`，请使用上表中的类型前缀，例如 `feat:`、`fix:`。

### 2. lint-staged 提示 `git add` 警告

lint-staged 新版本会自动暂存修改，无需在配置中写 `git add`。该警告不影响提交结果。

### 3. 换行符警告

项目统一使用 `LF` 换行符。如看到 `This file uses 'LF' line endings` 提示，可忽略或运行：

```bash
git config --global core.autocrlf false
```
